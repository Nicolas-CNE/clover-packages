NAME=busybox
VERSION=1.38.0
URL=https://busybox.net/downloads/busybox-1.38.0.tar.bz2
SOURCE_TYPE=makefile
DEPS=
BUILD_STEPS=make defconfig && sed -i 's/CONFIG_TC=y/# CONFIG_TC is not set/' .config && make -j$(nproc) && make CONFIG_PREFIX=$DESTDIR install
