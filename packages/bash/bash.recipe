NAME=bash
VERSION=5.3
URL=https://ftp.gnu.org/gnu/bash/bash-5.3.tar.gz
SOURCE_TYPE=autotools
DEPS=ncurses,readline
BUILD_STEPS=./configure --prefix=/usr --without-bash-malloc --with-installed-readline && make -j$(nproc) && make DESTDIR=$DESTDIR install
